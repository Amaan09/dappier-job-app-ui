import { Layout } from "../../shared/layout";

export const Home = () => {
    return (
        <Layout title="Dashboard">
            <p>Write about this application here.</p>
        </Layout>
    );
};
