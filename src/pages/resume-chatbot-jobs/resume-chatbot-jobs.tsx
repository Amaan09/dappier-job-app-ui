import { Layout } from "../../shared/layout";

export const ResumeChatbotJobs = () => {
    return (
        <Layout title="Chatbot Training Status">
            <p>
                This is the chatbot jobs page where you can see the status of
                the training of the chatbot.
            </p>
        </Layout>
    );
};
